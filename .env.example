# Optional. A GitHub personal access token (no scopes needed for public data).
# Without it you get 60 requests/hour per IP. With it, 5000/hour.
# Users can also paste their own token in the app UI (stored in localStorage only).
VITE_GITHUB_TOKEN=
